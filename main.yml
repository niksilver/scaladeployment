---

- name: Set up base EC2 instances
  hosts: localhost
  connection: local
  gather_facts: false

  roles:
    - ec2base

  post_tasks:
    - name: Debugging here and now
      debug: var=blah

- name: Install Java and Scala
  hosts: ec2hosts
  user: ubuntu
  gather_facts: true

  roles:
    - smola.java
